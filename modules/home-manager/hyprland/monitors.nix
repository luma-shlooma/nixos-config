{ ... }:

{
  # This is configuration for the home monitors specifically
  wayland.windowManager.hyprland = {
    settings = {
      monitor = [
        "DP-1, highrr, auto, 1"
        "HDMI-A-1, disable"
      ];
    };
  };
}
